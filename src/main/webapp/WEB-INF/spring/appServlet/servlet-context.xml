<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<mvc:annotation-driven />
	
	<!-- 파티로고 이미지 불러오기 -->
	<mvc:resources mapping="/logoImages/**" location="file:///C:/ProjectPM/ImageUpload/PartyLogo/"/>
	<!-- 파티메인 이미지 불러오기 -->
    <mvc:resources mapping="/mainImages/**" location="file:///C:/ProjectPM/ImageUpload/PartyMainImage/"/>
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<mvc:resources mapping="/resources/**" location="/resources/" />
    
	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<context:component-scan base-package="com.pm.myapp.controller" />
	<context:component-scan base-package="com.pm.myapp.exception" />
	<context:component-scan base-package="com.pm.myapp.service" />		

	<!-- 파일 업로드를 위한 설정을 추가-->
	<beans:bean
		id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<beans:property name="defaultEncoding" value="utf8"/>
		<beans:property name="uploadTempDir" value="file:/C:/temp/"/> <!-- 임시로 저장 -->
		<!-- <beans:property name="preserveFilename" value="true"/>  --> <!-- 파일이름유지 -->

		<beans:property name="maxUploadSize" value="10485760"/>                  <!-- 10MB -->
		
		<!-- <beans:property name="maxUploadSizePerFile" value="2097152"/>   -->           <!-- 2MB -->
		<!-- <beans:property name="maxInMemorySize" value="1048576"/>	-->  			 <!-- 1MB -->
		
	</beans:bean>
	

	
		<!-- Spring MVC interceptor 등록-->
<!-- <mvc:interceptors>

		<mvc:interceptor>
			** : 디렉터리가 몇개든지 모두 매칭되도록 한다 
			<mvc:mapping path="/sample/**/*" />

			<mvc:exclude-mapping path="/sample/doC" />
			mapping의 반대 <mvc:exclude-mapping path="" /> 이 경로로 들어오는 것들은 가로채지 않겠다

			이름을 지어 주지 않았어서 beans graph에 있는 이름으로(맨앞 소문자)
			<beans:ref bean="sampleInterceptor" />
		</mvc:interceptor>

	</mvc:interceptors> -->
	
	<!-- 단순한 뷰를 제공하는 매핑은 여기서 해준다 -->
<!-- 	<mvc:view-controller path="/user/login" status-code="200" view-name="/user/login" />
 -->	
</beans:beans>
