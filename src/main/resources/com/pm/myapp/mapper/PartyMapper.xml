<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pm.myapp.mapper.PartyMapper">
    
    <select id="getInfo" resultType="com.pm.myapp.domain.PartyVO">
    
    SELECT P.PTCODE, P.PTNAME, P.PSCORE, P.CDATE, P.LOGO, P.COVER, P.PPROF, P.PBPOINT, P.LCODE, H.HBCODE
    FROM PARTY P JOIN PARTYHB H
    ON P.PTCODE = H.PTCODE
    WHERE P.PTNAME =  #{PTNAME}

    </select>

</mapper>